---
title: "L14: Reutilización de estilos"
quiz:
  - question: "¿Qué hace la directiva `@apply` en TailwindCSS?"
    options:
      - "Permite aplicar animaciones CSS"
      - "Permite reutilizar clases utilitarias de TailwindCSS en una declaración CSS"
      - "Aplica estilos responsivos a los elementos"
    correctAnswer: "Permite reutilizar clases utilitarias de TailwindCSS en una declaración CSS"
    
  - question: "¿Qué clases se aplican en el botón usando `.button` con `@apply`?"
    options:
      - "bg-blue-500 text-black font-normal"
      - "bg-blue-500 text-white font-bold py-2 px-4 rounded"
      - "bg-gray-500 text-white font-light"
    correctAnswer: "bg-blue-500 text-white font-bold py-2 px-4 rounded"
    
  - question: "¿Qué clase define el estilo de un botón secundario reutilizando `@apply`?"
    options:
      - "button-primary"
      - "button-secondary"
      - "button-alert"
    correctAnswer: "button-secondary"

---

## Uso de la directiva @apply

La directiva `@apply` en TailwindCSS te permite reutilizar varias clases utilitarias y aplicarlas en una declaración CSS, lo que es útil cuando quieres usar las mismas clases en varios lugares sin repetirlas.

### Ejemplo básico de @apply



```html live
<style>
.button {
  @apply bg-blue-500 text-white font-bold py-2 px-4 rounded;
}
</style>
<button class="button">
  Botón con @apply
</button>
```

En este ejemplo, la clase `.button` reutiliza varias clases de TailwindCSS mediante `@apply` para evitar tener que repetirlas en cada lugar donde se utilice.

---

## Reutilización de estilos con @apply

### Reutilización en botones

Puedes definir un estilo base para los botones y luego reutilizarlo con variantes:

```css
/* archivo styles.css */
.button {
  @apply bg-blue-500 text-white font-bold py-2 px-4 rounded;
}

.button-primary {
  @apply bg-blue-500 hover:bg-blue-700;
}

.button-secondary {
  @apply bg-gray-500 hover:bg-gray-700;
}
```

```html live
<button class="button button-primary">
  Botón Primario
</button>
<button class="button button-secondary">
  Botón Secundario
</button>
```
